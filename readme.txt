When using the clean version,
you need to fill the Dockerfile in the frontend and backend folders
 and add a code file to the frontend and backend folders
and also add .env to the main folder with with all the ports you want to use
and the database user name. password and database name
